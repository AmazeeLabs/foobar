name: Test
on:
  push:
    branches:
      - dev

jobs:
  test:
    name: Test
    runs-on: ubuntu-20.04
    steps:
      - name: Setup
        uses: ./.github/actions/setup

      - name: Test
        run: pnpm test
